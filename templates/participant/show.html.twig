{% extends 'base.html.twig' %}

{% block title %}Particiapnt{% endblock %}

{% block body %}
    <h1>Participant</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ participant.user }}</td>
        </tr>

        <tr>
            <th>Date</th>
            <td>{{ participant.date ? participant.date|date('Y-m-d') : '' }}</td>
        </tr>

        </tbody>
    </table>

    <a href="{{ path('participant_index') }}">back to list</a>

    <a href="{{ path('edit', {'id': participant.id}) }}">edit</a>

    {{ include('partipant/_delete_form.html.twig') }}
{% endblock %}
