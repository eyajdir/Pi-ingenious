


{% extends 'admin/base.html.twig' %}
{% block nav %}
    {% include 'admin/include/navbar.html.twig' %}
{% endblock %}
{% block sidebar %}
    {% include 'admin/include/sidebar.html.twig' %}
{% endblock %}


{% block body %}










    <div class="content-page">
        <div class="container-fluid">
            <div class="row">
                <div class="col-xl-9 col-lg-8">
                    <div class="card">
                        <div class="card-header d-flex justify-content-between">
                            <div class="header-title">
                                <h4 class="card-title">Edit New  Offre:</h4>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="new-user-info">
                                {{ form_start(form) }}
                                <div class="row">
                                    <div class="form-group col-md-6">
                                        {{ form_row(form.nom, {"attr" : {'class':'form-control'},"label":"Nom Offre"})}}
                                    </div>
                                    <div class="form-group col-md-6">
                                        {{ form_row(form.logo, {"attr" : {'class':'form-control'},"label":"Image Offre"})}}
                                    </div>
                                    <div class="form-group col-md-6">
                                        {{ form_row(form.email, {"attr" : {'class':'form-control'},"label":"Email Offre"})}}
                                    </div>
                                    <div class="form-group col-md-6">
                                        {{ form_row(form.title, {"attr" : {'class':'form-control'},"label":"Title Offre"})}}
                                    </div>
                                    <div class="form-group col-md-6">
                                        {{ form_row(form.description, {"attr" : {'class':'form-control'},"label":"Description Offre"})}}
                                    </div>
                                    <div class="form-group col-md-6">
                                        {{ form_row(form.idcategorie, {"attr" : {'class':'form-control'},"label":"Idcategorie Offre"})}}
                                    </div>

                                </div>
                                <hr>
                                <div class="row">
                                </div>

                                <button type="submit" class="btn btn-primary">Edit New Offre</button>
                                {{ form_end(form) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    </div>
{% endblock %}
